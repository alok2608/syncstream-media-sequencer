# Base URL of the Go backend (no trailing slash).
VITE_API_URL=http://localhost:8080

# WebSocket endpoint. Optional: when omitted it is derived from VITE_API_URL by
# swapping http->ws / https->wss and appending /ws.
VITE_WS_URL=ws://localhost:8080/ws
